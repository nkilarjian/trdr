# Expo/React Native's Metro bundler expects a flat node_modules. pnpm's default
# isolated store breaks Metro's resolution of react/react-native internals, so
# the whole workspace uses a hoisted layout. (Node-side packages resolve via
# tsx + tsconfig paths and are unaffected.)
node-linker=hoisted
