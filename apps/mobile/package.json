{
  "name": "@trdr/mobile",
  "version": "0.0.0",
  "private": true,
  "description": "Expo (iOS + Android) client. Phase 1: scaffold Expo app, consume @trdr/core + @trdr/ui.",
  "scripts": {
    "placeholder": "echo \"@trdr/mobile — run `expo init` in Phase 1\""
  }
}
